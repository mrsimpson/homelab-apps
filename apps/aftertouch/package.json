{
  "name": "@homelab-apps/aftertouch",
  "version": "0.1.0",
  "private": true,
  "description": "Pulumi stack — deploys AfterTouch (Bose SoundTouch local cloud replacement) to the homelab Kubernetes cluster",
  "main": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@mrsimpson/homelab-core-components": "^0.2.2",
    "@pulumi/kubernetes": "^4.18.3",
    "@pulumi/pulumi": "^3.137.0"
  }
}
